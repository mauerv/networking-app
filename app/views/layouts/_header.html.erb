<div class='navbar has-background-primary'>
	<div class='navbar-brand'>
		<%= link_to('Home', root_path, class: 'navbar-item') %>
		<a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" @click="onMenuToggle">
		  <span aria-hidden="true"></span>
		  <span aria-hidden="true"></span>
		  <span aria-hidden="true"></span>
		</a>
	</div>
	<div class='navbar-menu' v-bind:class="{ 'is-active': isActive }">
		<div class='navbar-end'>
			<% if user_signed_in? %>
				<%= link_to('Profile', profile_path(current_user.profile), class: 'navbar-item has-background-primary') %>
				<%= link_to('Logout', destroy_user_session_path, method: :delete, class: 'navbar-item has-background-primary') %>
			<% else %>
				<%= link_to('Login', new_user_session_path, class: 'navbar-item has-background-primary') %>
				<%= link_to('Register', new_user_registration_path, class: 'navbar-item has-background-primary') %>
			<% end %>
		</div>
	</div>
</div>